<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spk">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icont"
        android:label="Pejam Apps"
        android:roundIcon="@drawable/icont"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainActivity"/>
        <activity android:name=".rtActivity" />
        <activity android:name=".desaActivity" />
        <activity android:name=".activity_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>